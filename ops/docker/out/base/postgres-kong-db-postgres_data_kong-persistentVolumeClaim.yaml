#! postgres-kong-db-postgres_data_kong-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: postgres-kong-db-postgres-data-kong
    namespace: docker
    labels:
        com.docker.compose.service: postgres-kong-db
        com.docker.compose.volume: postgres_data_kong
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
