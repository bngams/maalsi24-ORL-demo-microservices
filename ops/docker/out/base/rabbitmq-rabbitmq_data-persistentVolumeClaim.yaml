#! rabbitmq-rabbitmq_data-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: rabbitmq-rabbitmq-data
    namespace: docker
    labels:
        com.docker.compose.service: rabbitmq
        com.docker.compose.volume: rabbitmq_data
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
