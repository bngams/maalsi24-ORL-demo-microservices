#! postgres-kc-db-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: postgres-kc-db
    namespace: docker
    labels:
        com.docker.compose.project: docker
        com.docker.compose.service: postgres-kc-db
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: docker
            com.docker.compose.service: postgres-kc-db
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: docker
                com.docker.compose.service: postgres-kc-db
                com.docker.compose.network.microservices-network: "true"
        spec:
            containers:
                - name: keycloak-db
                  image: postgres:15
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: POSTGRES_DB
                      value: "keycloak"
                    - name: POSTGRES_PASSWORD
                      value: "keycloak"
                    - name: POSTGRES_USER
                      value: "keycloak"
                  livenessProbe:
                    exec:
                        command:
                            - /bin/sh
                            - -c
                            - pg_isready -U keycloak
                    periodSeconds: 10
                    timeoutSeconds: 5
                    failureThreshold: 5
                  ports:
                    - name: postgres-k-5432
                      containerPort: 5432
                  volumeMounts:
                    - name: var-lib-postgresql-data
                      mountPath: /var/lib/postgresql/data
            volumes:
                - name: var-lib-postgresql-data
                  persistentVolumeClaim:
                    claimName: postgres-kc-db-postgres-data-kc
